namespace WebX.Security.Shared.Constants
{
    public static class IdentityMessages
    {
        public const string INVALID_CREDENTIALS = nameof(INVALID_CREDENTIALS);
        public const string VERIFY_FAILED = nameof(VERIFY_FAILED);
        public const string VALIDATE_FAILED = nameof(VALIDATE_FAILED);
    }
}
